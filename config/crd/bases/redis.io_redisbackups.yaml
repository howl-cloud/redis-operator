---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: redisbackups.redis.io
spec:
  group: redis.io
  names:
    kind: RedisBackup
    listKind: RedisBackupList
    plural: redisbackups
    shortNames:
    - rb
    singular: redisbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.targetPod
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: RedisBackup is the Schema for the redisbackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RedisBackupSpec defines the desired state of a RedisBackup.
            properties:
              clusterName:
                description: ClusterName is the name of the RedisCluster to back up.
                type: string
              destination:
                description: Destination defines where to store the backup.
                properties:
                  s3:
                    description: S3 defines an S3-compatible storage destination.
                    properties:
                      bucket:
                        description: Bucket is the S3 bucket name.
                        type: string
                      endpoint:
                        description: Endpoint is the S3 endpoint URL (for non-AWS
                          S3 compatible stores).
                        type: string
                      path:
                        description: Path is the prefix/path within the bucket.
                        type: string
                      region:
                        description: Region is the AWS region.
                        type: string
                    required:
                    - bucket
                    type: object
                type: object
              method:
                default: rdb
                description: Method specifies the backup method.
                enum:
                - rdb
                - aof
                type: string
              target:
                default: prefer-replica
                description: Target specifies which pod to run the backup on.
                enum:
                - primary
                - prefer-replica
                type: string
            required:
            - clusterName
            type: object
          status:
            description: RedisBackupStatus defines the observed state of a RedisBackup.
            properties:
              backupSize:
                description: BackupSize is the size of the backup in bytes.
                format: int64
                type: integer
              completedAt:
                description: CompletedAt is when the backup completed.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error contains the error message if the backup failed.
                type: string
              phase:
                description: Phase is the current backup phase.
                type: string
              startedAt:
                description: StartedAt is when the backup started.
                format: date-time
                type: string
              targetPod:
                description: TargetPod is the pod that is running or ran the backup.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
